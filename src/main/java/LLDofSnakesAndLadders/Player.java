package LLDofSnakesAndLadders;

public class Player {
    String playerId;
    int currentPos;

    public Player(String playerId, int currentPos) {
        this.playerId = playerId;
        this.currentPos = currentPos;
    }
}
